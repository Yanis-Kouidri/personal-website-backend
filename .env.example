# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------

# Environment (development, production, test)
NODE_ENV=development

# The port the server should listen on
PORT=3000


# -----------------------------------------------------------------------------
# DATABASE (MongoDB)
# -----------------------------------------------------------------------------

# Connection string for your MongoDB database
# Format: mongodb://[username:password@]host[:port]/database
# Local example: mongodb://localhost:27017/my_app_db
MONGODB_URI=mongodb://localhost:27017/your_database_name


# -----------------------------------------------------------------------------
# SECURITY & AUTHENTICATION
# -----------------------------------------------------------------------------

# Secret key used to sign and verify JWT tokens.
# Use a long, random string (at least 32 characters).
# Command to generate one: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_here

# Secret key required to register a new user.
# This prevents random people from creating accounts on this private instance.
SIGNUP_KEY=your_private_invitation_key


# -----------------------------------------------------------------------------
# FRONTEND / CORS
# -----------------------------------------------------------------------------

# The URL of your frontend application (used by CORS middleware)
# Example for Dev: http://localhost:5173
# Example for prod: https://www.your-domain.com
ALLOWED_ORIGIN=http://localhost:5173